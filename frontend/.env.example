# Frontend Environment Variables
# Local development default (override in .env.local or Netlify site settings)
NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_WHATSAPP_NUMBER=919137290903
NEXT_PUBLIC_INSTAGRAM_URL=https://instagram.com

# Deployment note:
# - Set `NEXT_PUBLIC_API_URL` in your Netlify project to your backend's public API URL
#   (e.g. https://api.yoursite.com/api) so the live site fetches data from the database.
